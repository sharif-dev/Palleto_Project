<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.palleto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PopularpalletesActivity"></activity>
        <activity android:name=".Save_MakePalletActivity" />
        <activity android:name=".palletmaker_firstActivity" />
        <activity android:name=".CollectionActivity" />
        <activity android:name=".collection_pallets" />
        <activity android:name=".ColormeterActivity" />
        <activity android:name=".IntroduceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>